# 环境依赖同步报告

**检查日期**: 2026-06-05  
**检查范围**: `requirements.txt` / `Dockerfile` / `.env` / `common/clients/` / `services/retrieval/` / `app/worker/main.py` / `services/chat_agent/agent.py`

---

## 一、发现的问题与修复

### P0 🔴 依赖缺失 → 已修复

| # | 问题 | 影响 | 修复 |
|---|---|---|---|
| 1 | `requirements.txt` 缺少 `openai` | `llm.py` 中 `from openai import AsyncOpenAI` 必崩 | ✅ 添加 `openai>=1.50.0` |
| 2 | `requirements.txt` 缺少 `llama-index-core` / `llama-index-llms-qwen` | `agent.py` 中从 `llama_index.*` 导入必崩 | ✅ 替换为 `llama-index-core>=0.11.0` + `llama-index-llms-openai>=0.2.0`（项目统一 OpenAI 兼容） |
| 3 | `Dockerfile:25` 覆写 `pymilvus==2.4.4`，与 `requirements.txt` 的 2.4.6 冲突 | 实际装 2.4.4，API 可能不兼容 | ✅ 删除覆写行，统一 2.4.6 |
| 4 | `.env` 中 `MYSQL_PASSWORD=root` 与 `docker-compose.yml` 中 `MYSQL_ROOT_PASSWORD: "123456"` 不一致 | 容器启动后后端无法连接 MySQL | ✅ 改为 `MYSQL_PASSWORD=123456` |
| 5 | `.env` 中 `MYSQL_PORT=3307` 与 `docker-compose.yml` 的实际映射 `3307:3306` 一致，但容器内是 3306 | 容器内连接用 3306，宿主机用 3307。`.env` 写 3307 用于宿主机调试可接受，Docker volume 已注入 `MYSQL_PORT=3306` | ✅ 保持现状，容器环境变量已覆盖 |

### P1 🟡 模块缺失 → 已补全

| # | 问题 | 修复 |
|---|---|---|
| 6 | `common/clients/minio.py` 缺失 | ✅ 新建：MinIO 客户端，支持 PDF/图片上传、预签名 URL、桶管理 |
| 7 | `services/retrieval/` 仅剩 `CLAUDE.md`，核心代码缺失 | ✅ 补全 4 文件：`__init__.py` / `query_optimizer.py` / `searcher.py` / `reranker.py` |
| 8 | `app/worker/main.py` 仅监听队列，未注册 job handler | ✅ 重写：注册 `handle_ingest_job`，串联 parsing→indexing 流水线，含 stage 状态更新和异常处理 |
| 9 | `services/chat_agent/agent.py` 存在 3 处导入错误 | ✅ 修复：`llama_index.llms.qwen→openai`，`..retrieval.retriever→services.retrieval`，`..common.llm_client→common.clients.llm` |

---

## 二、当前依赖完整度

| 依赖包 | 用途 | 状态 |
|---|---|---|
| `openai>=1.50.0` | LLM / Embedding / Rerank 调用 | ✅ 已添加 |
| `llama-index-core>=0.11.0` | Agent 框架 / IngestionPipeline | ✅ 已添加 |
| `llama-index-llms-openai>=0.2.0` | LlamaIndex 的 OpenAI 兼容 LLM 适配 | ✅ 已添加（替代 qwen 适配器） |
| `pymilvus==2.4.6` | 向量库客户端 | ✅ 版本统一 |
| `minio==7.2.8` | 对象存储客户端 | ✅ |
| `rq==1.16.2` / `redis==5.0.8` | 任务队列 + 缓存 | ✅ |
| `mineru-kie-sdk==0.1.1` | MinerU 云端解析 | ✅ |
| `sentence-transformers==3.1.1` | 本地 embedding | ✅ |

---

## 三、新增模块清单

| 文件 | 行数 | 说明 |
|---|---|---|
| `common/clients/minio.py` | 105 | MinIO 客户端：PDF/图上传、桶管理、预签名URL |
| `services/retrieval/__init__.py` | 14 | 检索模块导出 |
| `services/retrieval/query_optimizer.py` | 140 | 查询优化：改写 + 翻译 + HyDE（并发） |
| `services/retrieval/searcher.py` | 158 | 混合检索：三路 dense + RRF 融合 |
| `services/retrieval/reranker.py` | 134 | 重排 + Corrective RAG 质量过滤 |
| `app/worker/main.py` | 105 | RQ worker：parsing→indexing 流水线 |

---

## 四、修正后的模块引用关系

```
agent.py (修复后)
  ├── llama_index.llms.openai (原 qwen，现在统一 OpenAI 兼容)
  ├── services/retrieval.searcher (原 ..retrieval.retriever)
  ├── services/retrieval.query_optimizer
  ├── common.clients.llm (原 ..common.llm_client)
  ├── common.config
  └── common.logging
```

---

## 五、后续需要验证

- `docker compose up -d --build` 容器启动是否正常
- `pip install -r requirements.txt` 是否无冲突
- 后端能否正确导入 `common/clients/minio.py` 和 `services/retrieval/` 模块
- 前端依赖无需变动（`package.json` 已有 axios/pinia/vue-router）
